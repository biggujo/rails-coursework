commit-msg:
  scripts:
    "commitlint.sh":
      runner: bash

pre-commit:
  commands:
    rails-linting:
      run: docker exec -it coursework-rails rake rubocop
    react-linting:
      run: docker exec -it coursework-react npm run lint
    react-styling:
      run: npx prettier frontend --write
